# Django
SECRET_KEY=fnka4*d)=5b5!lyzuykdnkf=xte&g&b-k_@-9tu49*7#g75bqp
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
#DATABASE_URL=postgresql://postgres:postgres@localhost:5435/hcs_cms_db
DATABASE_URL=postgresql://postgres:postgres@10.182.144.24:5433/hcs_cms_db


# CORS
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Payment gateway (frontend URLs after PG callback; defaults in config/settings/base.py)
# Existing-case document filing: return user to File Documents screen after online payment
# PG_DOCUMENT_FILING_REDIRECT_URL=http://localhost:4200/advocate/dashboard/efiling/document-filing/create

# JWT (SimpleJWT)
JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7
# Require verified email before token issue for self-registered party/advocate users
REGISTRATION_REQUIRE_EMAIL_VERIFICATION=False
THROTTLE_REGISTRATION=30/hour
THROTTLE_AUTH=120/hour

# Local API auth (DEBUG only; must match frontend `devAuthBypassToken` in src/app/environment.ts)
DEV_AUTH_BYPASS_TOKEN=local-hcs-cms-dev-only
DEV_AUTH_BYPASS_USERNAME=admin

# CIS 1.0 Legacy Database (read-only)
CIS_LEGACY_DB_NAME=cis_old_db
CIS_LEGACY_DB_USER=postgres
CIS_LEGACY_DB_PASSWORD=postgres
# CIS_LEGACY_DB_HOST=10.182.144.243
# CIS_LEGACY_DB_PORT=5432
CIS_LEGACY_DB_HOST=localhost
CIS_LEGACY_DB_PORT=5435
